<%= form_for @sales_party, url: {action: "create"} do |opp| %>
    <div class="panel panel-default">
        <div class="panel-heading">Sales Party - new record</div>
        <dl class="dl-horizontal">
            <dt><%= label_tag(:name) %></dt>
            <dd><%= opp.text_field :name %></dd>
        </dl>
    </div>
    <%= button_tag "Create", {class: "btn btn-primary opp submit", :method => "post"} %>
<% end %>
<%= button_to "To list", {action: "list"}, {class: "btn btn-primary opp cancel", :method => "get"} %>



